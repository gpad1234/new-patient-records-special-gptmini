// UI configuration for minimal diabetes pilot
// Set flags to enable/disable optional features
const config = {
  SHOW_RESEARCH: false,
  SHOW_DICOM: false,
  ALLOW_ADD_PATIENT: false, // when false, Add Patient is hidden and Import/Test Data is used
};

export default config;
